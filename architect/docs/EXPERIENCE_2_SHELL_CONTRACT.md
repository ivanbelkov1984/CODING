# Experience 2.0 — контракт оболочки (PR #185)

Документ фиксирует то, что было проверено в РЕАЛЬНОМ собранном бандле, а не
выведено из чтения исходника. Все фикстуры синтетические (`TEST-X2-*`).

---

## 0. Расследование «Ещё»

**Вопрос владельца:** на скриншоте в нижней навигации видно «Ещё», хотя тело PR
утверждает, что раздел упразднён.

**Что проверено.** Собранный бандл на точном HEAD ветки, Chromium, viewport
390×844, семь достижимых состояний. В каждом перечислены ВСЕ видимые ярлыки
нижней навигации и любой видимый элемент с текстом «Ещё».

| состояние | нижняя навигация | видимое «Ещё» |
|---|---|---|
| новый профиль (флаг не сохранён) | Сегодня · Дневник · Обзор · **Меню** | нет |
| существующий профиль (есть DB/CFG) | Сегодня · Дневник · Обзор · **Меню** | нет |
| `arch_nav_v2='1'` | Сегодня · Дневник · Обзор · **Меню** | нет |
| `arch_nav_v2='0'` (легаси) | таб-бара нет; боковой список: Сегодня · Сферы · Разум · Итоги · Здоровье · Астрология · Настройки · Обратная связь | нет |
| после перезагрузки | Сегодня · Дневник · Обзор · **Меню** | нет |
| сохранённое мусорное значение | Сегодня · Дневник · Обзор · **Меню** | нет |
| старая ссылка `#/more` | Сегодня · Дневник · Обзор · **Меню**, drawer открыт, hash переписан в `#/menu` | нет |

**Вывод.** На HEAD этой ветки «Ещё» не существует ни в разметке, ни в рантайме.
В нижней навигации ровно одна разметка (`#nsh-tabbar`), в ней написано «Меню».
Легаси-навигация (`#nav`) слова «Ещё» никогда не содержала — переименования
«в одном месте» не было и быть не могло.

**Откуда скриншот.** `data-nav="more"` / «Ещё» существует в `MAIN` (`ec7a9b2`,
`index.html:830`) — то есть в том, что сейчас в production. Ветка PR удаляет и
вкладку, и весь блок `ov-more`. Живой BUILD_SHA production на момент проверки
равен `ec7a9b2`, поэтому скриншот с «Ещё» — это текущий production, а не эта
ветка.

**Но одна настоящая ошибка в скриншоте всё же была** — см. §1: нижний таб-бар
рисовался ПОВЕРХ затемнения, поэтому при открытом меню он оставался видимым и
читался как часть меню. Это чинится здесь.

Защищено: `tests/x2shell.spec.mjs` §11 (6 проверок).

---

## 1. Мобильная шторка: что было сломано и что сделано

| дефект на входе | как было | как стало |
|---|---|---|
| под шторкой читался текст | `.sidebar` — стекло `rgba(17,23,41,.62)` | сплошной `var(--drawer-sf)` (`#0B1020` / `#FFFFFF`) |
| фон просвечивал | scrim `rgba(6,7,20,.5)` | `rgba(3,5,12,.92)` / `rgba(15,23,42,.86)` + `backdrop-filter: blur(8px)` |
| **нижний таб-бар просвечивал поверх затемнения** | `.scrim` z-index **55** < `.nsh-tabbar` z-index **58** | scrim поднят до **59**: таб-бар (58) < scrim (59) < drawer (60) < `.ov` (100); плюс таб-бар гаснет (`opacity:0; pointer-events:none`) |
| карточка «Отклик» висела поверх шторки | `.react-card` z-index 520, `body`-уровень | снимается при открытии меню |
| длительность вне полосы | `.3s` | `--drawer-dur: 220ms` (полоса 180–260ms), классом, без `setTimeout`; глобальное `prefers-reduced-motion` гасит |
| Escape не закрывал шторку | обработчик закрывал только `.ov.on` | Escape закрывает лист, если он есть, иначе шторку |
| фон оставался доступен клавиатуре | ничего | `inert` на всех `body`-детях, кроме `#sidebar`, `#scrim`, `#toasts` и `.ov` |
| фокус не уходил в шторку и не возвращался | ничего | фокус на первый видимый пункт при открытии, возврат на opener при закрытии |
| `aria-expanded` только на бургере | бургер | на реальном opener: и `#burger`, и вкладка «Меню» |

Закрытие работает четырьмя путями: тап по затемнению, Escape, выбор пункта,
навигация назад (hash). Все идут через один `closeNav()`.

Постоянный sidebar (iPad portrait 768–900 и desktop ≥901) — не drawer:
`inert`, ловушка фокуса и затемнение там намеренно не применяются
(`navIsDrawer()`).

Защищено: `tests/x2shell.spec.mjs` §10 (24 проверки, тёмная и светлая тема).
Нечитаемость доказывается не на глаз: со скриншота берётся полоса фона справа
от шторки и считается разброс яркости — текст даёт резкие перепады, заглушенный
фон нет (порог `sd < 9`; фактически 3.4 тёмная / 1.0 светлая).

---

## 2. Карта: старая оболочка → новая → маршрут → форма → писатель → коллекция

Experience 2.0 — это ОБОЛОЧКА. Она не создаёт своих форм и своих писателей.

| раздел | старый вход | новый вход | маршрут | форма | писатель | каноническая коллекция |
|---|---|---|---|---|---|---|
| Дневник | `#nav` «Разум» | «Дневник» + Action Island | `goTo('map')`, `msub('overview')` | `ov-add` | `saveIns()` | `insights` |
| Сны | подвкладка | свой пункт «Сны» | `goTo('map');msub('dreams')` | `ov-drm` | `saveDrm()` | `dreams` (+ лента `insights`) |
| Психология | подвкладка | «Психология» / «Цели» | `goTo('map');msub('psychology')` | `ov-moment`, `ov-why` | `saveMoment()`, `saveWhy()` | `moments`, `psy*` |
| Закономерности | подвкладка | свой пункт | `goTo('map');msub('patterns')` | `ov-pat-add` | существующий | `patterns` |
| Здоровье | `#nav` «Здоровье» | «Здоровье» + остров | `goTo('health')` | `ov-measure`, `ov-symptom` | `saveMeasure()`, `saveSymptom()` | `measures`, `symptoms` |
| Цели | внутри психологии | свой пункт + доводка до блока | `goTo('map');msub('psychology')` | существующая | существующий | `psyGoals` |
| Астрология | `#nav` «Астрология» | «Астрология» | `goTo('astro')` | существующая | существующий | профиль/натал |
| Источники | `ov-more` | «Источники и импорт» | `openExtImport()` | существующая | существующий | `externalConnections` |
| Резервные копии | `ov-more` | «Резервные копии» | `openEncBackup()` | ESM-модуль | существующий | — |
| Инспектор | `ov-more` | «Инспектор записей» | `openRecords()` | существующая | существующий | — |
| Правки | внутри инспектора | там же | — | существующая | существующий | ревизии записи |

**A. Использует ли новый UI те же формы, обработчики и писателей?** Да. Каждый
пункт sidebar и каждая кнопка Action Island вызывают существующий `goTo`,
`msub`, `openOv(id)` или существующий опенер. Ни одной новой формы не заведено.

**B. Есть ли сущность, которая при разных оболочках попадает в разные
коллекции?** Ноль. Проверено записью: одна и та же сущность сохраняется при
`navshell=ON` и при `navshell=OFF`, и сравнивается НАБОР выросших коллекций.
Для инсайта, сна, измерения и момента набор совпадает. (У сна набор — две
коллекции, `dreams` и лента `insights`: так было и до Experience 2.0, оболочка
этого не меняет.)

**C. Заведены ли дубли форм или писателей специально под X2?** Нет. Проверяется
и позитивно (writer — одна глобальная функция в обеих оболочках), и негативно
(в `window` нет ни одного имени вида `x2*save|write|persist`).

**D. Правки ходят в обе стороны?** Да. Правка, сделанная в новой оболочке,
читается легаси-оболочкой из ХРАНИЛИЩА (а не из памяти) байт-в-байт.

Защищено: `tests/x2shell.spec.mjs` §8 (9 проверок) и §9 (1 проверка).

---

## 3. Решение по feature-flag

Experience 2.0 — **основная оболочка**. Пользовательский тумблер «Новая
навигация (эксперимент)» из Настроек убран: два конкурирующих UI, между
которыми выбирает пользователь, — это не продукт.

Аварийный откат сохранён и остаётся рабочим, но становится **внутренним**:
`localStorage arch_nav_v2 = '0'` (или вызов `toggleNavShell()`). Легаси-код
навигации намеренно НЕ удалён — удаление осталось бы невозвратным изменением
в одном большом диффе.

Защищено: `tests/e2e.mjs` (нет пользовательского тумблера; откат работает в обе
стороны), `tests/nav-integration.spec.mjs`.

---

## 4. Наблюдение (не менялось в этом PR)

`esc(s) = (s||'').replace(...)` падает на нечисловой-нестроковом значении.
Через production-писателей это недостижимо: `saveMeasure()` кладёт `name` и
`value` строками, схема одна (SCHEMA_VERSION 9), restore и sync возвращают то,
что записал тот же писатель. Дефект был найден только потому, что тестовая
фикстура была написана в неканонической форме — фикстура исправлена, `esc`
намеренно оставлен без изменений (за пределами объёма этого PR).


---

## 5. Сокращение IA: 19 пунктов → 8 пространств

**Принцип.** В глобальном меню живёт только то, КУДА человек идёт целиком.
Быстрые действия, вторичные экраны, отчёты, инструменты и сервисные функции
переезжают внутрь своего родительского пространства. Ни одна функция не
удалена и не спрятана — у каждой есть новый достижимый путь.

### Глобальное меню (8)

| группа | пункты |
|---|---|
| Жизнь | Главная · Дневник |
| Работа с собой | Психология · Здоровье |
| Анализ | Астрология · Закономерности |
| Данные | Источники |
| Система | Настройки |

Нижний таб-бар: Сегодня · Дневник · ＋ · Психология · Меню. Вкладка «Обзор»
убрана — второй непонятный home не должен конкурировать с Главной.

### Карта переездов

| было в меню | route / функция | новый родитель | новый путь | writer |
|---|---|---|---|---|
| Главная | `goTo('home')` | — | остаётся в меню | — |
| Дневник | `goTo('map');msub('overview')` | — | остаётся в меню | `saveIns` |
| Психология | `goTo('map');msub('psychology')` | — | остаётся в меню | `saveMoment` |
| Здоровье | `goTo('health')` | — | остаётся в меню | `saveMeasure` |
| Астрология | `goTo('astro')` | — | остаётся в меню | существующий |
| Закономерности | `goTo('map');msub('patterns')` | — | остаётся в меню | существующий |
| Источники и импорт | `openExtImport()` | — | остаётся в меню как «Источники» | существующий |
| Настройки | `goTo('settings')` | — | остаётся в меню | — |
| **Записать** | `openCapture()` | глобальный ввод | FAB таб-бара · ＋ в шапке · блок «Действия» на Главной · Action Island | существующие capture-формы |
| **Сны** | `goTo('map');msub('dreams')` | Дневник | Меню → Дневник → «Сны» (2 действия) | `saveDrm` |
| **Сферы** | `goTo('vit')` | Главная | Главная → «Сферы» · Action Island «Отметить сферу» · `#/spheres` | существующие |
| **Цели** | `msub('psychology')` + доводка | Главная / Психология | Главная → «Цели» → блок целей психологии | существующие |
| **Отчёт врачу** | `openOv('ov-doc-report')` | Здоровье | Здоровье → «Отчёт врачу» (кнопка «Дневника тела») | существующие |
| **Обзор** | `goTo('sys')` | Главная | Главная → «Обзор недели» · `#/overview` | — |
| **Поиск** | `openOv('ov-search')` | шапка | иконка поиска в topbar на всех экранах | — |
| **Инспектор записей** | `openRecords()` | запись / Настройки | Настройки → Данные → «Мои записи»; вход из записи | — |
| **Резервные копии** | `openEncBackup()` | Настройки | Настройки → Данные → «Зашифрованная резервная копия» | — |
| **Профили** | `openProfiles()` | карточка профиля | карточка профиля в шторке; Настройки → Профиль | — |
| **Обратная связь** | `openOv('ov-feedback')` | Настройки | Настройки → Поддержка → «Обратная связь» | — |

Ни один route, handler или writer не удалён и не переписан. Изменилось только
то, откуда на них попадают.

### Почему Сны — в Дневнике, а не в Психологии

Решено по коду и данным, а не по названию: `ms-dreams` — подвкладка `pg-map`
в группе поднавигации «Записи»; `saveDrm()` кладёт сон и в `dreams`, и в ленту
`insights` (то есть в поток дневника). Перенос в Психологию потребовал бы
менять route и разорвал бы связь сна с лентой, в которую он уже попадает.
Критерий владельца выполнен: сон достижим за 2 действия от меню.

### Главная: иерархия вместо десяти одинаковых карточек

«Жизнь сейчас» подаётся тремя группами, а не плоским списком:

- **Требует внимания** — Незавершённое, Цели;
- **Жизнь** — Здоровье, Психология, Сны, Сферы, События жизни;
- **Понимание и данные** — Закономерности, Обзор недели, Источники.

Каждый блок ведёт в своё родительское пространство. Новых коллекций нет —
всё через `projAll` поверх существующих.

### Action Island

Только действия, не навигация: Дневник — Запись/Событие; Сны — Записать сон;
Психология — Момент/«Зачем?»; Здоровье — Измерение/Симптом; Сферы — Отметить.
На Главной острова нет намеренно: «Записать» там уже даёт FAB и блок
«Действия», третий вход тем же словом закрывал бы собой карточки.
Плавающий остров теперь резервирует под себя место (`body.has-island`) —
до этого он накрывал последнюю карточку экрана.

### Доказательства

| проверка | что доказано |
|---|---|
| `x2shell` §12 | в меню ровно 8 утверждённых пространств; все 11 вторичных функций из меню убраны |
| `x2shell` §13 | достижимость через UI: путь начинается с меню/шапки/экрана, а не с прямого вызова роута |
| `x2shell` §14 | 7 legacy deep links живы; неизвестный hash безопасно ведёт на Главную |
| `x2shell` §15 | все 8 пунктов видны в шторке без прокрутки (запас 0px на iPhone 390) |
| `x2shell` §16 | остров не закрывает контент; 1–3 действия; не дублирует пункты меню |
| `x2shell` §8–§9 | writers и коллекции те же в обеих оболочках; правки ходят в обе стороны |


---

## 6. PR D — визуальный hardening оболочки

### Найденный дубль: два плавающих бара действий

PR #185 добавил Action Island (`#x2-island`), **не заметив уже существующий**
`context-action-dock` (issue #138). В production они показывались одновременно,
друг над другом, с одними и теми же действиями:

| экран | dock (66px) | island (88px) |
|---|---|---|
| Здоровье | Симптом · Измерение · Тяга | Измерение · Симптом |
| Сны | Записать сон | Записать сон |
| Сферы | Отметить сферу · Новая сфера | Отметить сферу |
| Закономерности | Новый паттерн | Паттерн |

Дубль устранён удалением **моего** слоя: `context-action-dock` старше, богаче
(в нём есть «Тяга», «Новая сфера», «Отчёт врачу»), уже покрыт тестами и уже
прячет дублирующие инлайн-кнопки. Единственный ключ, которого у него не было —
`map:psychology` — перенесён в его реестр (те же обработчики `ov-moment` и
`ov-why`). Ни одно действие не потеряно.

### Нижняя навигация — плавающий островок

| было | стало |
|---|---|
| полоса во всю ширину, приклеена к нижнему краю | капсула **70% ширины**, по 15% пустого поля слева и справа, центрирована |
| `background: var(--sf)` — стекло `rgba(17,23,41,.62)`, фон просвечивал | `var(--island-sf)` — сплошной `#121a2e` / `#FFFFFF` |
| `border-top`, прямые углы | `border-radius: 999px`, тень `--island-sh` |
| `bottom: 0` | `bottom: calc(env(safe-area-inset-bottom) + 12px)` — висит, а не приклеен |
| ＋ отдельно от панели | ＋ — центр капсулы, кольцо `0 0 0 4px var(--island-sf)` собирает его с ней |

**Состав:** Сегодня · Дневник · ＋ · Психология · **Астрология**. «Меню» из
нижнего слоя убрано полностью — его единственный вход в шапке справа
(`#topbar-menu`); дублировать один destination в двух постоянных слоях
навигации незачем.

Подписи в островке скрыты: пять подписанных слотов в 70% ширины пришлось бы
сжимать до нечитаемого кегля, а этого владелец запретил отдельно. Доступное имя
каждой вкладки живёт в `aria-label`, полный список с названиями — в шторке.
Возврат подписей — одна строка CSS (`.nsh-tab span{display:flex}` + увеличение
ширины), если решение окажется неверным.

### Верхняя панель

`padding: max(env(safe-area-inset-top),14px) … var(--s3)` + `min-height:52px`
давали на iPhone с Dynamic Island шапку около 120px: safe-area и собственный
отступ складывались. Теперь `padding-top: calc(env(safe-area-inset-top) + 8px)`,
`padding-bottom: 8px`, `min-height: 44px` — вырез резервирует safe-area, а сверх
неё ровно 8px спокойной дистанции. Итог: **61px против 71px на MAIN** (замер
brand-v3). `.top-acts` получил свой ритм; ＋ из шапки на телефоне скрыт — он
дублировал центр островка.

Размер фирменного знака **не трогался**: 36px — зафиксированный бренд-ориентир
(brand-v3 требует 36–48px). Первая попытка ужать его до 30px уронила 9 проверок
брендовой сюиты и была отменена: шапка стала компактнее за счёт отступов, а не
за счёт бренда.

Итоговый ряд шапки: знак · заголовок раздела · Поиск · Настройки · Меню.

### Dock приведён к тому же языку

Тот же дефект прозрачности, что был у навигации: `background: var(--sf)` +
`backdrop-filter: blur(18px)`. Заменено на `--island-sf` / `--island-bd` /
`--island-sh`, радиус `999px`, ширина `min(86vw, 420px)`, позиция поднята на
`84px + safe-area`, чтобы не лежать на островке навигации.

### Старый и новый интерфейс — фактическое состояние

Проверено записью в реальном бандле, не чтением:

| вопрос | ответ |
|---|---|
| Что получает пользователь без сохранённого выбора | **новую оболочку** (`navshell`), пользовательского тумблера нет |
| Один ли writer | `saveIns` в обеих оболочках — **тот же объект функции** (`===`), не копия |
| Куда попадает запись | один и тот же набор коллекций в обеих оболочках |
| Куда попадает исправление | effective value одинаков в обеих (`2` после правки `1 → 2`) |
| Разные ли экраны | **нет:** 7 страниц `.pg` в обеих; различается только хром навигации |

«Старый интерфейс» — не интерфейс, а **вариант навигационного хрома** над тем же
набором страниц, форм и writer-ов. Расщепления поведения нет и структурно быть
не может: страницы одни, обработчики одни, `persist` один.
