# FINAL COMPLETION SEQUENCE

This document intentionally does not contain private user data.

The product-completion program is not considered closed merely because Waves 0–9 are merged. The remaining owner-approved sequence is:

1. Continuous GPT / Google Drive Bridge using the stable Wave 6/7/8 external-work contracts.
2. Controlled owner-side private backfill of «МОЯ ЖИЗНЬ» with provenance, semantic source identity, deduplication and claim-class safeguards.
3. Final integration / product / release audit against the real production MAIN after the backfill acceptance.

Hard rules:
- Public GitHub may contain code, schemas, synthetic fixtures and aggregate non-identifying acceptance counts only.
- Real LIFE/DREAM/PARA/PSY/health/relationship text, source IDs, chat IDs, Drive content, personal names and private evidence must never be committed to this public repository.
- Canonical records remain the source of truth; the bridge must not create a second life ledger or duplicate EVENT_SOURCE.
- Stable source identity is semantic sourceId; source module/chat/session are provenance, not identity.
- Existing sourceRefs[], claimClasses[], transactional preview/commit, profile isolation, sync, tombstones and encrypted-backup semantics must remain intact.
- No deduplication by text.
- No claim may be silently promoted from user report / experience / symbolic interpretation / working hypothesis to established fact.
- Each implementation phase uses a fresh branch from current MAIN and a Draft PR. No merge or auto-merge before independent owner acceptance.

The concrete execution contracts are tracked in the final issues created from this sequence. The umbrella #145 should be closed only after all final contracts are accepted against production MAIN.
