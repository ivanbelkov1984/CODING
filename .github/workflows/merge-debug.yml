name: Merge debug

on:
  pull_request:
    paths:
      - "architect/**"
      - ".github/workflows/merge-debug.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: architect
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: architect/package-lock.json
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - name: Capture full test output
        id: test
        shell: bash
        run: |
          set +e
          npm test > merge-debug.log 2>&1
          code=$?
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: merge-debug-log
          path: architect/merge-debug.log
          if-no-files-found: error
      - name: Keep failure status
        if: steps.test.outputs.exit_code != '0'
        run: exit 1
